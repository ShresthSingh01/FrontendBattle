---
phase: 01-project-scaffold-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.js
  - postcss.config.js
  - index.html
  - src/index.css
  - src/main.jsx
  - src/App.jsx
  - src/data/pricingMatrix.js
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-07
must_haves:
  truths:
    - "The dev server starts and compiles the application successfully"
    - "index.html includes correct SEO metadata, title, and Google Fonts links"
    - "Tailwind colors and fonts are extended with brand tokens matching spec"
    - "The directory structure matches the layout requirements"
  artifacts:
    - path: "index.html"
      provides: "SEO metadata and Google Fonts loading"
      contains: "NexaFlow AI — Automate Everything. Scale Instantly."
    - path: "tailwind.config.js"
      provides: "Tailwind config with brand tokens"
      contains: "forsythia"
    - path: "src/index.css"
      provides: "Global styles and custom properties"
      contains: "--forsythia"
    - path: "src/data/pricingMatrix.js"
      provides: "Pricing matrix data source"
      contains: "PRICING_MATRIX"
  key_links:
    - from: "index.html"
      to: "src/main.jsx"
      via: "script tag script type=module"
      pattern: "src/main.jsx"
    - from: "src/main.jsx"
      to: "src/index.css"
      via: "CSS import"
      pattern: "import.*index\\.css"
---

<objective>
Scaffold the React + Vite project, configure Tailwind CSS v3 with brand tokens, load Google Fonts, create the folder structure, and add SEO meta tags.

Purpose: Establishes the project base and SEO foundation for high competition scoring.
Output: Initialized React project with compiled Tailwind, preconnected fonts, and full SEO metadata.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-seo/01-CONTEXT.md
@.planning/phases/01-project-scaffold-seo/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React + Vite Project safely</name>
  <files>package.json, vite.config.js</files>
  <read_first>package.json, vite.config.js</read_first>
  <action>
    Create a temporary directory named temp-scaffold.
    Run `npx create-vite@latest temp-scaffold --template react --no-interactive` to initialize the project.
    Copy all files and folders (except .git or existing planning files) from temp-scaffold to the root directory.
    Delete the temp-scaffold directory.
  </action>
  <verify>Test-Path package.json</verify>
  <acceptance_criteria>
    - package.json exists in root
    - vite.config.js exists in root
  </acceptance_criteria>
  <done>React + Vite project scaffolded in workspace root</done>
</task>

<task type="auto">
  <name>Task 2: Setup and configure Tailwind CSS v3</name>
  <files>tailwind.config.js, postcss.config.js, package.json</files>
  <read_first>package.json, tailwind.config.js, postcss.config.js</read_first>
  <action>
    Install Tailwind CSS v3.4.4, postcss, and autoprefixer as dev dependencies.
    Generate/write tailwind.config.js and postcss.config.js.
    Configure tailwind.config.js content list and extend colors and fonts according to D-02:
    - Colors: arctic-powder, mystic-mint, forsythia, deep-saffron, nocturnal-exp, oceanic-noir, true-black, off-white, mid-gray, border-dark, border-light.
    - Fonts: mono (JetBrains Mono), sans (Inter).
    - Transition: ease-entry (cubic-bezier(0.25, 0.46, 0.45, 0.94)).
  </action>
  <verify>npm run build runs and compiles tailwind classes</verify>
  <acceptance_criteria>
    - package.json contains tailwindcss, postcss, and autoprefixer
    - tailwind.config.js contains colors mapping to CSS variables
    - postcss.config.js is configured with tailwindcss and autoprefixer
  </acceptance_criteria>
  <done>Tailwind CSS v3 installed and brand tokens configured</done>
</task>

<task type="auto">
  <name>Task 3: Configure index.html with SEO Meta Tags & Fonts</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    Modify index.html to add preconnect to fonts.googleapis.com and fonts.gstatic.com.
    Import Google Fonts: JetBrains Mono and Inter.
    Include exact `<title>` and meta tags from SECTION 0 of NUCLEAR_PROMPT_V2.md:
    - Title: "NexaFlow AI — Automate Everything. Scale Instantly."
    - Description: "NexaFlow AI is the next-generation data automation platform that eliminates manual workflows, accelerates pipelines, and delivers enterprise-grade AI at startup speed."
    - Robots: "index, follow"
    - Canonical: "https://your-url.vercel.app/"
    - OG tags: og:title, og:description, og:type, og:image, og:url
    - Twitter tags: twitter:card, twitter:title, twitter:description
  </action>
  <verify>index.html contains exact meta tag content</verify>
  <acceptance_criteria>
    - index.html contains all 11 required SEO and font loading tags
  </acceptance_criteria>
  <done>SEO meta tags and Google Fonts set up in index.html</done>
</task>

<task type="auto">
  <name>Task 4: Setup index.css and directory structure</name>
  <files>src/index.css, src/data/pricingMatrix.js</files>
  <read_first>src/index.css, src/data/pricingMatrix.js</read_first>
  <action>
    Write src/index.css containing tailwind directives and :root CSS variables block matching §2 of NUCLEAR_PROMPT_V2.md.
    Create directories: src/components/Layout, src/components/sections, src/data, src/hooks, src/components/ui.
    Write src/data/pricingMatrix.js using the template provided in Section 7 of NUCLEAR_PROMPT_V2.md (or export an empty/placeholder object if not fully detailed in §7, but make sure to declare export const PRICING_MATRIX).
    Clean up boilerplate assets (delete src/assets, clean App.css and App.jsx to render a clean base layout).
  </action>
  <verify>Test-Path src/components/Layout, src/data/pricingMatrix.js</verify>
  <acceptance_criteria>
    - src/index.css contains :root variable declarations
    - src/data/pricingMatrix.js contains PRICING_MATRIX definition
    - Directories are successfully created
  </acceptance_criteria>
  <done>CSS variables and project directory structure initialized</done>
</task>

</tasks>

<verification>
- [ ] npm run build compiles successfully
- [ ] index.html parses with all requested SEO tags
- [ ] src/index.css contains all brand custom properties
</verification>

<success_criteria>
- All tasks completed successfully
- Project builds without errors
- Brand token setup matches spec
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-seo/01-01-SUMMARY.md`
</output>
